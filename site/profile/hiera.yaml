---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "OS Family YAML"
    path: "%{facts.os.family}-%{facts.os.release.major}.yaml"

  - name: "common"
    path: "common.yaml"
